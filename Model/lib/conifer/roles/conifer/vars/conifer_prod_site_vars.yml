modelconfig_accountDb_connectionUrl: 'jdbc:oracle:oci:@{{ acctdb_shortname }}'
modelconfig_accountDb_login: '{{ acctdb_login }}'
modelconfig_accountDb_password: '{{ acctdb_password }}'

modelconfig_appDb_connectionUrl: 'jdbc:oracle:oci:@{{ appdb_shortname }}'
modelconfig_appDb_login: '{{ appdb_login }}'
modelconfig_appDb_password: '{{ appdb_password }}'

modelconfig_userDb_login: '{{ userdb_login }}'
modelconfig_userDb_password: '{{ userdb_password }}'
modelconfig_userDb_connectionUrl: 'jdbc:oracle:oci:@{{ userdb_shortname }}'



appdb_shortname: "{{ lookup('colfile',
    hostname + '
    col=appDb_name
    src=https://cbilsvn.pmacs.upenn.edu/svn/apidb/websiteconf/master_configuration_set'
  ) }}"
appdb_login: "{{ lookup('colfile',
    hostname + '
    col=appDb_login
    src=https://cbilsvn.pmacs.upenn.edu/svn/apidb/websiteconf/master_configuration_set'
  ) }}"
appdb_password: "{{ lookup('euparc',
  'attr=password xpath=database/user[@login=\"' + appdb_login + '\"]'
  )
}}"

acctdb_shortname: "{{ lookup('colfile',
    hostname + '
    col=acctDb_name
    src=https://cbilsvn.pmacs.upenn.edu/svn/apidb/websiteconf/master_configuration_set'
  ) }}"
acctdb_login: "{{ lookup('colfile',
    hostname + '
    col=acctDb_login
    src=https://cbilsvn.pmacs.upenn.edu/svn/apidb/websiteconf/master_configuration_set'
  ) }}"
acctdb_password: "{{ lookup('euparc',
  'attr=password xpath=database/user[@login=\"' + acctdb_login + '\"]'
  )
}}"


userdb_shortname: "{{ lookup('colfile',
    hostname + '
    col=userDb_name
    src=https://cbilsvn.pmacs.upenn.edu/svn/apidb/websiteconf/master_configuration_set'
  ) }}"
userdb_login: "{{ lookup('colfile',
    hostname + '
    col=userDb_login
    src=https://cbilsvn.pmacs.upenn.edu/svn/apidb/websiteconf/master_configuration_set'
  ) }}"
userdb_password: "{{ lookup('euparc',
  'attr=password xpath=database/user[@login=\"' + userdb_login + '\"]'
  )
}}"

